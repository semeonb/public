from setuptools import setup, find_packages

setup(
    name="airmelt_ai pakages",
    version="0.1.2",
    description="A collection of utilities and data manipulation packages",
    author="Semeon Balagula",
    packages=find_packages(),  # Automatically finds all packages in the project
    install_requires=[],  # Add any external dependencies here
)
